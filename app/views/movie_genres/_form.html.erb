<%= form_with(model: movie_genre) do |form| %>
  <% if movie_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie_genre.errors.count, "error") %> prohibited this movie_genre from being saved:</h2>

      <ul>
        <% movie_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="form-group">
      <%= form.label :name, "Nome" %>
      <%= form.text_field :name, class: "form-control"%>
    </div>
    
    <div class="form-btn">
      <%= link_to "Cancel", movie_genres_path, class: "btn btn-danger cancel" %>
      <%= form.submit "Salvar", class: "btn btn-danger primary" %>
    </div>
  </form>
<% end %>
