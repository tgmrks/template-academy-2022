<%= form_with(model: director) do |form| %>
  <% if director.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
        <% director.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="form-group">
      <%= form.label :name, "Nome" %>
      <%= form.text_field :name, class: "form-control"%>
    </div>

    <div class="form-btn">
      <%= link_to "Cancel", directors_path, class: "btn btn-danger cancel" %>
      <%= form.submit "Salvar", class: "btn btn-danger primary" %>
    </div>
  </form>
<% end %>
